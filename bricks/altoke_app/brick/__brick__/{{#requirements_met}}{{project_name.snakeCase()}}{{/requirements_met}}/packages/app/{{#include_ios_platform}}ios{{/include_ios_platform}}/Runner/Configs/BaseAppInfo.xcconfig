// The base name of the app
APP_NAME_BASE = {{project_name.titleCase()}}

// The base product bundle identifier
PRODUCT_BUNDLE_IDENTIFIER_BASE = {{ios_bundle_identifier}}

// The resolved app name
APP_NAME = $(APP_NAME_PREFIX)$(APP_NAME_BASE)

// The resolved product bundle identifier
PRODUCT_BUNDLE_IDENTIFIER = $(PRODUCT_BUNDLE_IDENTIFIER_BASE)$(PRODUCT_BUNDLE_IDENTIFIER_SUFFIX)
