name: altoke_reactive_caches
description: In-memory reactive caches.
version: 0.0.1+1
publish_to: none

environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  collection: ^1.18.0
  altoke_common:
    #*w 1v 4> w*#
    #*replace-start*#
    path: ../../altoke_common/reference/
    #*with*#
    # path: ../common/
    #*replace-end*#
  #*w 1v 2> w*#
  meta: ^1.11.0
  rxdart: ^0.28.0

dev_dependencies:
  test: ^1.25.2
  very_good_analysis: ^5.1.0
#*w 1v w*#
#*drop*#
# TODO(mrverdant13): Implement portal comment for code fragment relocation.
# ignore_for_file: sort_pub_dependencies
