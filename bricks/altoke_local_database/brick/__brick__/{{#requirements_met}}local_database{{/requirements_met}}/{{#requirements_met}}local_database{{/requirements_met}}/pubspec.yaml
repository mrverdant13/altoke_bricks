name: {{#requirements_met}}local_database{{/requirements_met}}
description: An interface for a persistent local database.
version: 0.0.1+1
publish_to: none

environment:
  sdk: ">=3.12.1 <4.0.0"

dependencies:
  common:
    path: ../../common/
  meta: ^1.18.0

dev_dependencies:
  test: ^1.31.1
  very_good_analysis: ^10.0.0
