name: altoke_local_database
description: A template for a local database.
version: 0.0.1

environment:
  mason: ^0.1.0

vars:
  local_database_alternative:
    type: enum
    values:
      - drift
      - hive
      - sembast
    description: The local database alternative.
    default: drift
    prompt: What local database alternative do you want to use?
