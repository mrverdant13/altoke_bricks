import 'package:{{objects.snakeCase()}}_drift_storage/{{objects.snakeCase()}}.drift.dart';

/// A Drift (SQLite) companion class for {{objects.titleCase()}}.
typedef Drift{{objects.pascalCase()}}Companion = {{objects.pascalCase()}}Companion;

/// A Drift (SQLite) DAO for {{objects.titleCase()}}.
typedef Drift{{objects.pascalCase()}}Dao = {{objects.pascalCase()}}Drift;
