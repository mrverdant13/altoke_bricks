name: {{objects.snakeCase()}}_realm_storage
description: A persistent storage for {{objects.titleCase()}} built on top of Realm.
version: 0.0.1+1
publish_to: none

environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  common:
    path: ../../common/
  {{objects.snakeCase()}}_storage:
    path: ../{{objects.snakeCase()}}_storage/
  {{object.snakeCase()}}:
    path: ../../{{object.snakeCase()}}/
  collection: ^1.18.0
  meta: ^1.11.0
  realm: ^2.1.0

dev_dependencies:
  test: ^1.25.2
  very_good_analysis: ^5.1.0
