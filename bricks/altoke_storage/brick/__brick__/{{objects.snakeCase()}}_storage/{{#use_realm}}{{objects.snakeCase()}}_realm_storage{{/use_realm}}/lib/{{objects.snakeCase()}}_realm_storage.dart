/// A persistent storage for {{objects.titleCase()}} built on top of Realm.
library {{objects.snakeCase()}}_realm_storage;

export 'src/{{objects.snakeCase()}}_filter.dart';
export 'src/{{objects.snakeCase()}}_realm_storage.dart';
export 'src/realm_{{object.snakeCase()}}.dart';
