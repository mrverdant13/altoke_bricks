/// An interface for a persistent storage of {{objects.titleCase()}}.
library {{objects.snakeCase()}}_storage;

export 'src/{{objects.snakeCase()}}_storage.dart';
export 'src/failures/failures.dart';
