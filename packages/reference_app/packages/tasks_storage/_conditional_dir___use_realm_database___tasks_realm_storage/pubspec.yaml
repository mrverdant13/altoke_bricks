name: tasks_realm_storage
description: A persistent storage for tasks built on top of Realm.
version: 0.0.1+1
publish_to: none

environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  common:
    path: ../../common/
  meta: ^1.11.0
  realm: ^1.9.0
  tasks:
    path: ../../tasks/
  tasks_storage:
    path: ../tasks_storage/

dev_dependencies:
  test: ^1.25.2
  very_good_analysis: ^5.1.0
