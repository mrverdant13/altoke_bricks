name: element_in_memory_cache
description: An in-memory cache for a single element.
version: 0.0.1+1
publish_to: none

environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  meta: ^1.10.0
  rxdart: ^0.27.7

dev_dependencies:
  test: ^1.25.2
  very_good_analysis: ^5.1.0
