import 'package:riverpod_annotation/riverpod_annotation.dart';

part 'scoped_task_id.pod.g.dart';

@Riverpod(
  dependencies: [],
)
int scopedTaskId(ScopedTaskIdRef ref) {
  throw UnimplementedError(
    '`${scopedTaskIdPod.name}` not initialized',
  );
}
