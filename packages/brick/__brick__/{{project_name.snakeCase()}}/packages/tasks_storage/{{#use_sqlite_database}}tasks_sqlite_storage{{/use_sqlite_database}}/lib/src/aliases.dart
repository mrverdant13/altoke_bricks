import 'package:tasks_sqlite_storage/tasks.drift.dart';

/// A SQLite table for tasks.
typedef DriftTasksTable = Tasks;

/// A SQLite data class for tasks.
typedef DriftTask = Task;

/// A SQLite companion class for tasks.
typedef DriftTasksCompanion = TasksCompanion;

/// A SQLite DAO for tasks.
typedef DriftTasksDao = TasksDrift;
