name: local_brick_installer
description: A tool to install bricks managed by a local monorepo.
publish_to: none

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  monorepo_elements: any

dev_dependencies:
  very_good_analysis: ^5.1.0
