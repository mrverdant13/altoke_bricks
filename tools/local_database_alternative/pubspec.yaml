name: local_database_alternative
description: Local database alternative.
version: 0.0.1+1
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  mason: ^0.1.1

dev_dependencies:
  very_good_analysis: ^7.0.0
