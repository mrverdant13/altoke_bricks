name: brick_generator
description: A generator for bricks.
publish_to: none

environment:
  sdk: ">=3.5.1 <4.0.0"

dependencies:
  meta: ^1.15.0
  monorepo_elements:
    path: ../monorepo_elements
  path: ^1.9.0
  shell:
    path: ../shell

dev_dependencies:
  very_good_analysis: ^6.0.0
